import React from 'react';
import PropTypes from 'prop-types';

import Navbar from '../navbar/Navbar';

const Home = (props) => {
  return <div>
      <Navbar />
  </div>;
};

Home.propTypes = {};

export default Home;
